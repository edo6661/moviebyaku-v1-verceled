// export const movieApiSlice = apiSlice.injectEndpoints({
// 	endpoints: (build) => ({
// 		popularMovies: build.query({
// 			query: (page = 1) => `movie/popular?page=${page}`,
// 		}),
// 	}),
// });

// export const { usePopularMoviesQuery } = movieApiSlice;
